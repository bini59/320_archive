import { DatabaseSync } from "node:sqlite";
import { randomUUID } from "node:crypto";
import { ARCHIVE_LIST_MAX_PAGE, ARCHIVE_LIST_PAGE_SIZE, ARCHIVE_SEARCH_MAX_TOKENS, ARCHIVE_SEARCH_QUERY_MAX_LENGTH, ARCHIVE_STATUSES, CAPTURE_FAILURE_MESSAGES, type Archive,type ArchiveRepository,type CaptureFailureCode,type PublicArchiveItem,type PublicArchiveQuery,type Snapshot,type Tag } from "./types";

type Row=Record<string,unknown>;
const str=(v:unknown)=>typeof v==='string'?v:null;
function toArchive(row:Row,tags:Tag[]=[]):Archive { const status=str(row.status); if(!ARCHIVE_STATUSES.includes(status as never))throw new Error("Unknown status"); return {id:String(row.id),originalUrl:String(row.original_url),normalizedUrl:String(row.normalized_url),status:status as Archive["status"],createdAt:String(row.created_at),snapshot:status==='saved'?{title:str(row.title),description:str(row.description),capturedAt:String(row.captured_at),finalUrl:String(row.final_url),byteLength:Number(row.byte_length)}:null,failureCode:str(row.failure_code) as CaptureFailureCode|null,failureMessage:str(row.failure_message),tags}; }

export class SqliteArchiveRepository implements ArchiveRepository {
  private readonly database:DatabaseSync;
  constructor(path:string){this.database=new DatabaseSync(path);this.database.exec("PRAGMA journal_mode=WAL; PRAGMA busy_timeout=5000;");this.migrate();}
  private migrate(){
    this.database.exec("BEGIN IMMEDIATE");
    try {
      const existing=this.database.prepare("SELECT sql FROM sqlite_master WHERE type='table' AND name='archives'").get() as Row|undefined;
      const legacy=this.database.prepare("SELECT name FROM sqlite_master WHERE type='table' AND name='archives_v1'").get();
      if(existing && !String(existing.sql).includes("'saved'") && !legacy)this.database.exec("ALTER TABLE archives RENAME TO archives_v1");
      this.database.exec(`CREATE TABLE IF NOT EXISTS archives(id TEXT PRIMARY KEY,original_url TEXT NOT NULL,normalized_url TEXT NOT NULL UNIQUE,status TEXT NOT NULL CHECK(status IN ('pending','saved','failed')),created_at TEXT NOT NULL,title TEXT,description TEXT,captured_at TEXT,final_url TEXT,byte_length INTEGER,failure_code TEXT,failure_message TEXT) STRICT;
        CREATE TABLE IF NOT EXISTS capture_events(id INTEGER PRIMARY KEY, created_at INTEGER NOT NULL) STRICT;
        CREATE TABLE IF NOT EXISTS storage_budget(singleton INTEGER PRIMARY KEY CHECK(singleton=1),used_bytes INTEGER NOT NULL,reserved_bytes INTEGER NOT NULL) STRICT;
        CREATE TABLE IF NOT EXISTS budget_reservations(id TEXT PRIMARY KEY,bytes INTEGER NOT NULL,created_at INTEGER NOT NULL,expires_at INTEGER NOT NULL) STRICT;
        INSERT OR IGNORE INTO storage_budget VALUES(1,0,0);`);
      this.database.exec(`CREATE TABLE IF NOT EXISTS tags(id INTEGER PRIMARY KEY,name TEXT NOT NULL,slug TEXT NOT NULL UNIQUE) STRICT;
        CREATE TABLE IF NOT EXISTS archive_tags(archive_id TEXT NOT NULL REFERENCES archives(id) ON DELETE CASCADE,tag_id INTEGER NOT NULL REFERENCES tags(id) ON DELETE CASCADE,PRIMARY KEY(archive_id,tag_id)) STRICT;
        CREATE VIRTUAL TABLE IF NOT EXISTS archive_search USING fts5(archive_id UNINDEXED,title,original_url,body);
        CREATE INDEX IF NOT EXISTS archives_public_order ON archives(status,captured_at DESC,id DESC);
        CREATE INDEX IF NOT EXISTS archive_tags_by_tag ON archive_tags(tag_id,archive_id);`);
      const reservationSql=this.database.prepare("SELECT sql FROM sqlite_master WHERE type='table' AND name='budget_reservations'").get() as Row;
      if(!String(reservationSql.sql).includes("expires_at")){this.database.exec("ALTER TABLE budget_reservations ADD COLUMN expires_at INTEGER NOT NULL DEFAULT 0");}
      if(this.database.prepare("SELECT name FROM sqlite_master WHERE type='table' AND name='archives_v1'").get())this.database.exec("INSERT OR IGNORE INTO archives(id,original_url,normalized_url,status,created_at) SELECT id,original_url,normalized_url,status,created_at FROM archives_v1; DROP TABLE archives_v1;");
      this.database.exec(`INSERT INTO archive_search(archive_id,title,original_url,body)
        SELECT id,COALESCE(title,''),original_url,'' FROM archives a WHERE status='saved' AND NOT EXISTS(SELECT 1 FROM archive_search f WHERE f.archive_id=a.id);
        PRAGMA user_version=4; COMMIT`);
    } catch(error){this.database.exec("ROLLBACK");throw error;}
  }
  private tags(id:string):Tag[]{return Array.from((this.database.prepare("SELECT t.name,t.slug FROM tags t JOIN archive_tags at ON at.tag_id=t.id WHERE at.archive_id=? ORDER BY t.slug") as any).all(id) as Iterable<Row>).map((r:Row)=>({name:String(r.name),slug:String(r.slug)}));}
  private addTags(id:string,tags:Tag[]){for(const tag of tags){this.database.prepare("INSERT INTO tags(name,slug) VALUES(?,?) ON CONFLICT(slug) DO NOTHING").run(tag.name,tag.slug);this.database.prepare("INSERT OR IGNORE INTO archive_tags(archive_id,tag_id) SELECT ?,id FROM tags WHERE slug=?").run(id,tag.slug);}}
  createOrGet(input:{originalUrl:string;normalizedUrl:string;tags?:Tag[]}){this.database.exec("BEGIN IMMEDIATE");try{const result=this.database.prepare("INSERT OR IGNORE INTO archives(id,original_url,normalized_url,status,created_at) VALUES(?,?,?,?,?)").run(randomUUID(),input.originalUrl,input.normalizedUrl,"pending",new Date().toISOString());const row=this.database.prepare("SELECT * FROM archives WHERE normalized_url=?").get(input.normalizedUrl) as Row;this.addTags(String(row.id),input.tags??[]);this.database.exec("COMMIT");return {archive:toArchive(row,this.tags(String(row.id))),created:Number(result.changes)===1};}catch(e){this.database.exec("ROLLBACK");throw e;}}
  findById(id:string){const row=this.database.prepare("SELECT * FROM archives WHERE id=?").get(id);return row?toArchive(row as Row,this.tags(id)):null;}
  markSaved(id:string,s:Snapshot,indexText="",tags:Tag[]=[]){this.database.exec("BEGIN IMMEDIATE");try{const result=this.database.prepare("UPDATE archives SET status='saved',title=?,description=?,captured_at=?,final_url=?,byte_length=?,failure_code=NULL,failure_message=NULL WHERE id=? AND status='pending'").run(s.title,s.description,s.capturedAt,s.finalUrl,s.byteLength,id);if(Number(result.changes)===1){this.addTags(id,tags);this.database.prepare("DELETE FROM archive_search WHERE archive_id=?").run(id);const original=String((this.database.prepare("SELECT original_url FROM archives WHERE id=?").get(id) as Row).original_url);this.database.prepare("INSERT INTO archive_search(archive_id,title,original_url,body) VALUES(?,?,?,?)").run(id,s.title??"",original,indexText);}this.database.exec("COMMIT");return this.findById(id);}catch(e){this.database.exec("ROLLBACK");throw e;}}
  markFailed(id:string,code:CaptureFailureCode,message:string){const safe=CAPTURE_FAILURE_MESSAGES[code]===message?message:CAPTURE_FAILURE_MESSAGES.capture_failed;this.database.prepare("UPDATE archives SET status='failed',failure_code=?,failure_message=? WHERE id=? AND status='pending'").run(code,safe,id);return this.findById(id);}
  listPublic(input:PublicArchiveQuery){const page=Math.min(ARCHIVE_LIST_MAX_PAGE,Math.max(1,Number.isSafeInteger(input.page)?input.page!:1));const tag=(input.tag??"").slice(0,32);const tokens=(input.q??"").slice(0,ARCHIVE_SEARCH_QUERY_MAX_LENGTH).normalize("NFKC").match(/[\p{L}\p{N}]+/gu)?.slice(0,ARCHIVE_SEARCH_MAX_TOKENS)??[];const match=tokens.map(t=>`"${t.replaceAll('"','""')}"`).join(" AND ");const joins=`${match?"JOIN archive_search f ON f.archive_id=a.id":""} ${tag?"JOIN archive_tags at ON at.archive_id=a.id JOIN tags filter_tag ON filter_tag.id=at.tag_id":""}`;const where=`a.status='saved' ${match?"AND archive_search MATCH ?":""} ${tag?"AND filter_tag.slug=?":""}`;const params=[...(match?[match]:[]),...(tag?[tag]:[])];const total=Number((this.database.prepare(`SELECT COUNT(DISTINCT a.id) value FROM archives a ${joins} WHERE ${where}`).get(...params) as Row).value);const rows=Array.from((this.database.prepare(`SELECT DISTINCT a.id,a.original_url,a.title,a.description,a.captured_at FROM archives a ${joins} WHERE ${where} ORDER BY a.captured_at DESC,a.id DESC LIMIT ? OFFSET ?`) as any).all(...params,ARCHIVE_LIST_PAGE_SIZE,(page-1)*ARCHIVE_LIST_PAGE_SIZE) as Iterable<Row>);const items:PublicArchiveItem[]=rows.map(row=>({id:String(row.id),originalUrl:String(row.original_url),title:str(row.title),description:str(row.description),capturedAt:String(row.captured_at),tags:this.tags(String(row.id))}));return {items,total,page,pageSize:ARCHIVE_LIST_PAGE_SIZE,pageCount:Math.ceil(total/ARCHIVE_LIST_PAGE_SIZE)};}
  reserveBudget(i:{windowMs:number;maxSubmissions:number;maxStoredBytes:number;reserveBytes:number;timeoutMs:number}){this.database.exec("BEGIN IMMEDIATE");try{const now=Date.now(),id=randomUUID(),expiresAt=now+i.timeoutMs+30_000;this.database.prepare("DELETE FROM capture_events WHERE created_at < ?").run(now-i.windowMs);this.database.prepare("DELETE FROM budget_reservations WHERE expires_at <= ?").run(now);const count=Number((this.database.prepare("SELECT COUNT(*) AS value FROM capture_events").get() as Row).value);const budget=this.database.prepare("SELECT used_bytes FROM storage_budget WHERE singleton=1").get() as Row;const reserved=Number((this.database.prepare("SELECT COALESCE(SUM(bytes),0) AS value FROM budget_reservations").get() as Row).value);if(count>=i.maxSubmissions||Number(budget.used_bytes)+reserved+i.reserveBytes>i.maxStoredBytes){this.database.exec("ROLLBACK");return null;}this.database.prepare("INSERT INTO capture_events(created_at) VALUES(?)").run(now);this.database.prepare("INSERT INTO budget_reservations(id,bytes,created_at,expires_at) VALUES(?,?,?,?)").run(id,i.reserveBytes,now,expiresAt);this.database.exec("COMMIT");let done=false;return {release:()=>{if(done)return;const result=this.database.prepare("DELETE FROM budget_reservations WHERE id=?").run(id);if(Number(result.changes)===1)done=true;},commit:(bytes:number)=>{if(done)return false;this.database.exec("BEGIN IMMEDIATE");try{const exists=this.database.prepare("SELECT id FROM budget_reservations WHERE id=?").get(id);const result=exists?this.database.prepare("UPDATE storage_budget SET used_bytes=used_bytes+? WHERE singleton=1 AND used_bytes+?<=?").run(bytes,bytes,i.maxStoredBytes):{changes:0};this.database.prepare("DELETE FROM budget_reservations WHERE id=?").run(id);this.database.exec("COMMIT");done=true;return Number(result.changes)===1;}catch(error){this.database.exec("ROLLBACK");throw error;}}};}catch(e){this.database.exec("ROLLBACK");throw e;}}
  close(){this.database.close();}
}
